---
import cn from "classnames";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div
  class={cn(
    "card bg-card dark:bg-black-shade rounded-2xl overflow-hidden",
    className,
  )}
>
  <slot />
</div>
